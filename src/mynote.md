Note
//a props a means by which a parent comp and shared data with the child component
    //how a parent can send prop to children  -- done using Key ={}
    //how a child can pick up prop from the parent and use it

    // props rules
    // props must not be mutated by the child 

    //prop drilling--

    //never store any data that will keep changing in variable in react rather use state
    //state is used to keep a data that will be changing: react will update it automatically in everywhere the state is used

    //Hooks are special function that let us manage state -useState
    //state are immutable
    //